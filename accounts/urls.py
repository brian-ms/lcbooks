# No longer needed after downloading the allauth app and using its stuff instead
"""from django.urls import path
from .views import SignupPageView

urlpatterns = [
    path("signup/", SignupPageView.as_view(), name="signup"),
]
"""
